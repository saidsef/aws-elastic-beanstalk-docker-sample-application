packages:
  yum:
    nginx: []
services:
  sysvinit:
    nginx:
      enabled: true
      ensureRunning: true
      sources: ["/etc/nginx"]
      commands:
        restart_nginx:
          command: /etc/init.d/nginx restart
